context p_escaped_keywords {
	// 'context', 'type' and 'event' are keywords - here used as identifiers via the '^' escape.

	module ^context.^type {
		type String
		value-object ^event {
			String value
		}
		value-object Foo {
			^event data
		}
	}
}
